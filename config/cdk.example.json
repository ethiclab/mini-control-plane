{
  "$comment": "Config locale (gitignored) popolato da mini cdk. Questo file è l'example trackato. Copia in `config/cdk.json` se vuoi valori di default statici, altrimenti lascia che `mini cdk` lo popoli automaticamente dalle scelte interattive.",
  "bundles": {
    "onceui": {
      "repo": "eleven/onceui",
      "buildScript": "scripts/build-aws.sh"
    },
    "backoffice-ui": {
      "repo": "eleven/backoffice-ui",
      "buildScript": "scripts/build-aws.sh"
    },
    "survey-ui": {
      "repo": "eleven/survey-ui",
      "buildScript": "scripts/build-aws.sh"
    }
  }
}
