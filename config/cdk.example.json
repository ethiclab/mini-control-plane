{
  "$comment": "Copia questo file in config/cdk.json (gitignored) e inserisci i tuoi valori reali. profiles = profili AWS per i tuoi domini; bundles = webapp buildabili (repo relativo alla root del cluster, ~/<cluster>).",
  "profiles": {
    "example-test": {
      "profile": "example-test",
      "account": "000000000000",
      "region": "eu-west-1",
      "rootDomain": "test.example.com",
      "defaultZoneName": "test.example.com"
    },
    "example-prod": {
      "profile": "example-prod",
      "account": "000000000000",
      "region": "eu-west-1",
      "rootDomain": "example.com",
      "defaultZoneName": "example.com"
    }
  },
  "bundles": {
    "app": {
      "repo": "cluster/app-ui",
      "buildScript": "scripts/projects/app/buildStatic.sh"
    }
  }
}
